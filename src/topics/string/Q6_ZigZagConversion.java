package topics.string;

import java.util.ArrayList;
import java.util.List;

/**
 * @author FELIX
 * <p>
 * Questuin: The string "PAYPALISHIRING" is written in a zigzag pattern
 * on a given number of rows like this: (you may want to display this
 * pattern in a fixed font for better legibility) P A H N A P L S I I G
 * Y I R And then read line by line: "PAHNAPLSIIGYIR" Write the code
 * that will take a string and make this conversion given a number of
 * rows: string convert(string s, int numRows);
 * <p>
 * Example 1: Input: s = "PAYPALISHIRING", numRows = 3 Output:
 * "PAHNAPLSIIGYIR"
 * <p>
 * Example 2: Input: s = "PAYPALISHIRING", numRows = 4 Output:
 * "PINALSIGYAHRPI" Explanation: P I N A L S I G Y A H R P I
 */
public class Q6_ZigZagConversion {
    public static void main(String[] args) {
        System.out.println(new Q6_ZigZagConversion().convert("PAYPALISHIRING", 3));
    }

    public String convert(String s, int numRows) {
        return null;
    }
}